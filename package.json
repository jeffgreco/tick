{
  "name": "tick",
  "version": "0.1.0",
  "description": "Watch faces, animations, and alerts for a Raspberry Pi round touch display",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4",
    "puppeteer": "^24.37.2",
    "vite": "^6.1.0"
  }
}
